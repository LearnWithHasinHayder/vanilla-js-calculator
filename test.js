const Course = {
    name:"Mastering Vue.js",
    batch:"1st Batch"
}

const {name, batch} = Course
// const name = Course.name
// const name = Course.name
// const batch = Course.batch

console.log(name, batch)